scry has been deployed.

  Image:      {{ include "scry.image" . }}
  Transport:  {{ .Values.transport }} on {{ .Values.listen }}
  Replicas:   {{ .Values.replicaCount }}

{{- if .Values.ingress.enabled }}

Ingress is enabled. Configured hosts:
{{- range .Values.ingress.hosts }}
  - {{ .host }}
{{- end }}
{{- else }}

Service is ClusterIP-only. Port-forward to reach scry locally:

  kubectl port-forward svc/{{ include "scry.fullname" . }} 7777:{{ .Values.service.port }}

Then point your MCP client at http://localhost:7777.
{{- end }}

For full docs see: https://github.com/felixgeelhaar/scry
