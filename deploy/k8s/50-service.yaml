apiVersion: v1
kind: Service
metadata:
  name: scry
  namespace: scry
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: scry
  ports:
    - name: mcp
      port: 7777
      targetPort: mcp
      protocol: TCP
